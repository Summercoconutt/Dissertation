You are helping me build a robust DAO-level clustering validation and debugging pipeline for my dissertation project.

Project context:
- I have a DAO-level dataset where each row is one DAO.
- The goal is NOT to force clusters, but to rigorously test whether meaningful cluster structure actually exists in the data.
- Previous K-means results suggested an extremely imbalanced 3-cluster solution (one very large cluster, one small cluster, one single-DAO outlier), so I now need a more rigorous pipeline.
- I want code that is clean, modular, reproducible, and suitable for research reporting.

Please write Python code (preferably using pandas, numpy, scikit-learn, matplotlib, seaborn if necessary, and optionally hdbscan if installed) that performs the following steps.

==================================================
1. INPUT AND DATA HANDLING
==================================================
- Read a DAO-level feature table from a CSV or Parquet file.
- Make the input path easy to configure at the top of the script.
- Save all outputs into a configurable output directory.
- Print basic dataset info:
  - number of rows
  - number of columns
  - feature names
  - missing values summary
- Restrict clustering to a specified list of numeric feature columns that I can manually edit.

==================================================
2. PREPROCESSING
==================================================
- Create a preprocessing pipeline suitable for clustering:
  - optional log1p transform for skewed positive variables
  - robust scaling using RobustScaler
- Allow me to define which columns should receive log1p transformation.
- Drop rows with missing values in selected clustering features, but print how many rows were dropped.
- Save the cleaned dataset used for clustering.

==================================================
3. FEATURE DISTRIBUTION DIAGNOSTICS
==================================================
For each selected feature:
- Plot histogram + KDE
- Save each plot separately
- Compute and save summary statistics:
  - mean
  - median
  - std
  - min
  - max
  - skewness
  - kurtosis
- If possible, add a simple indication of whether the distribution looks heavily skewed.

Goal:
- Help assess whether the data may be continuous / heavy-tailed rather than naturally clustered.

==================================================
4. CORRELATION AND REDUNDANCY CHECK
==================================================
- Compute correlation matrix for selected features
- Save:
  - full correlation matrix CSV
  - heatmap PNG
- Identify highly correlated feature pairs using a threshold (e.g. absolute correlation > 0.8)
- Save a CSV listing those pairs
- Print a warning if many features are highly correlated

Goal:
- Diagnose whether multicollinearity is making clustering unstable or low-dimensional.

==================================================
5. OUTLIER DETECTION
==================================================
I need a rigorous outlier detection section because previous clustering may have been dominated by extreme DAOs.

Please implement:
- z-score based outlier detection for each feature
- IQR-based outlier detection for each feature
- an aggregate outlier flag:
  - mark a DAO as an outlier if it exceeds a configurable threshold (for example, z-score > 3 in any feature OR outside IQR bounds in multiple features)
- Save:
  - full outlier report CSV
  - cleaned dataset without flagged outliers
- Print:
  - number of outliers detected
  - DAO IDs / names if available

Important:
- Keep both versions of the dataset:
  1. original cleaned dataset
  2. outlier-removed dataset

==================================================
6. PCA EXPLORATION
==================================================
For both datasets (with outliers and without outliers):
- Fit PCA on the scaled feature matrix
- Save:
  - explained variance ratio
  - cumulative explained variance plot
  - PCA scatter plot for PC1 vs PC2
- If DAO names/IDs are available, optionally annotate extreme points
- Save PCA loadings table so I can interpret what PC1 and PC2 represent

Goal:
- Check whether the data forms clear separated groups or mostly one dense cloud
- Understand whether variation is mainly driven by scale/activity/whale concentration etc.

==================================================
7. K-MEANS CLUSTERING VALIDATION
==================================================
For both datasets (with and without outliers), run K-means for k in {2,3,4,5,6}.

For each k:
- compute inertia
- compute silhouette score
- compute Calinski-Harabasz index
- compute Davies-Bouldin index
- save cluster sizes
- save cluster labels back to the dataset

Also generate and save:
- elbow curve
- silhouette vs k plot
- CH vs k plot
- DB vs k plot

For the selected k values, save:
- cluster size table
- cluster centroid table in original feature scale
- cluster centroid table in standardized scale
- boxplots of each feature by cluster
- PCA scatter colored by cluster

Important:
- Use fixed random_state for reproducibility
- Print warnings when cluster sizes are extremely imbalanced
- Explicitly flag cases like:
  - one cluster has only 1 DAO
  - one cluster contains more than 90% of observations

Goal:
- Diagnose whether K-means is finding meaningful groups or just isolating outliers.

==================================================
8. HIERARCHICAL CLUSTERING
==================================================
- Run hierarchical agglomerative clustering with Ward linkage
- Save dendrogram
- For k in {2,3,4,5,6}, compute the same validation metrics where applicable:
  - silhouette
  - CH
  - DB
  - cluster sizes
- Save cluster assignments
- Compare cluster assignments with K-means using cross-tab tables

Goal:
- Check whether the same broad structure appears across methods.

==================================================
9. DENSITY-BASED CLUSTERING
==================================================
Please also run DBSCAN and, if available, HDBSCAN.

For DBSCAN:
- try a sensible grid of eps and min_samples values
- save number of clusters found
- save number of noise points
- save cluster labels

For HDBSCAN (if package is installed):
- run HDBSCAN with a few sensible settings
- save cluster labels
- save cluster probabilities if available
- save number of clusters and noise points

Goal:
- Test whether the data has any natural density-based cluster structure without forcing k.
- If the result is “one cluster + noise”, that is still an important research finding.

==================================================
10. NULL / BASELINE COMPARISON
==================================================
I want a stronger test of whether the observed clustering is better than random structure.

Please implement a simple null benchmark:
- create synthetic random datasets with the same number of rows and columns
- for each feature, sample from a normal distribution using the observed mean and std, OR permute each real feature independently
- run K-means on these null datasets for the same k range
- compare silhouette / CH / DB between real data and null data
- save summary tables and plots

Goal:
- Assess whether the real data shows stronger clustering than would be expected by chance.

==================================================
11. STABILITY ANALYSIS
==================================================
I want to know whether the clustering is stable.

Please implement:
- repeated K-means runs across different random seeds
- optionally bootstrap resampling
- compare cluster stability across runs using adjusted Rand index (ARI)
- save:
  - ARI summary table
  - stability plots if possible

Goal:
- If clustering is unstable, that should be clearly shown.

==================================================
12. RESEARCH-READY OUTPUTS
==================================================
Please generate a final summary report in CSV and plain text / markdown including:
- sample sizes before and after outlier removal
- feature skewness summary
- highly correlated features
- outlier counts
- K-means validation metrics by k
- hierarchical clustering metrics
- DBSCAN / HDBSCAN results
- null comparison results
- stability results

Also produce a short automatically generated interpretation file that states, in cautious academic language:
- whether strong cluster structure appears to exist
- whether the data seems continuous / heavy-tailed
- whether outliers dominate the clustering
- whether clustering results are robust across methods

Important:
- Do NOT overclaim.
- If the evidence suggests weak cluster structure, say so explicitly.
- Write the interpretation in a dissertation-friendly tone.

==================================================
13. CODE STRUCTURE
==================================================
Please organize the code cleanly, ideally with functions such as:
- load_data()
- preprocess_data()
- plot_feature_distributions()
- compute_correlation_diagnostics()
- detect_outliers()
- run_pca()
- run_kmeans_validation()
- run_hierarchical_validation()
- run_density_clustering()
- run_null_benchmark()
- run_stability_analysis()
- write_summary_report()

Please include:
- clear comments
- sensible error handling
- reproducible random seeds
- easy-to-edit configuration variables at the top

==================================================
14. FILE NAMING
==================================================
Use clear file names such as:
- cleaned_dataset.csv
- cleaned_dataset_no_outliers.csv
- feature_summary.csv
- correlation_matrix.csv
- high_corr_pairs.csv
- outlier_report.csv
- pca_explained_variance.csv
- kmeans_metrics.csv
- hierarchical_metrics.csv
- dbscan_results.csv
- null_benchmark.csv
- stability_summary.csv
- clustering_interpretation.md

==================================================
15. FINAL REQUIREMENT
==================================================
Please produce code that is suitable for dissertation analysis, not just quick exploration.
I want the pipeline to help answer this research question:

“Do DAO-level governance features exhibit meaningful cluster structure, or do they instead form a continuous spectrum dominated by scale, participation, and outliers?”

Start by writing the full Python script.
After that, briefly explain:
1. how to run it
2. which input columns I need to edit
3. where the outputs will be saved